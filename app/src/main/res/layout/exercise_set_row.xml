<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools">
	
	<data>
		
		<variable
			name="model"
			type="com.piotr.practicepad.exerciseList.ExerciseSetEntity" />
		
		<variable
			name="checkBoxHandler"
			type="com.piotr.practicepad.exerciseList.CheckBoxHandler" />
		
		<variable
			name="navigationHandler"
			type="com.piotr.practicepad.exerciseList.NavigationHandler" />
	</data>
	
	<LinearLayout
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:background="@color/grey"
		android:onClick="@{() -> navigationHandler.navigationClick(model.id)}"
		android:orientation="horizontal">
		
		<TextView
			android:id="@+id/title"
			android:layout_width="0dp"
			android:layout_height="wrap_content"
			android:layout_gravity="center"
			android:layout_margin="@dimen/medium_margin"
			android:textSize="@dimen/medium_text_size"
			android:layout_weight="1"
			android:text="@{model.name}"
			android:textAppearance="?attr/textAppearanceListItem"
			tools:text="Example text" />
		
		<CheckBox
			android:id="@+id/checkbox"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:layout_gravity="center"
			android:layout_margin="16dp"
			android:buttonTint="@color/white"
			android:checked="@{checkBoxHandler.shouldBeChecked(model.id)}"
			android:onClick="@{() -> checkBoxHandler.checkBoxClick(model.id)}" />
	
	</LinearLayout>
</layout>
