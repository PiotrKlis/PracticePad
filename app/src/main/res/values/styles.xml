<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/grey_top</item>
        <item name="colorPrimaryDark">@color/grey_top</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/white</item>
    </style>
    
    <style name="ToolbarTheme">
        <item name="android:textColorPrimary">@color/grey_top</item>
        <item name="android:textColorSecondary">@color/black_ish</item>
        <item name="android:textColor">@color/black_ish</item>
    </style>`

</resources>
